<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>TrackOne</title>
  <link rel="stylesheet" href="/css/main.css" />
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
</head>
<body>
  <div class="background-overlay"></div>
  <nav class="nav-bar">
    <div class="nav-left">
      <a href="/" class="logo">TrackOne</a>
    </div>
    <div class="nav-right">
      <a href="/experiences">Experiences</a>

      <% if (!user) { %>
        <a href="/auth/sign-in">Sign In</a>
        <a href="/auth/sign-up">Sign Up</a>
      <% } else { %>
        <span></span>  <img
    src="https://api.dicebear.com/9.x/initials/svg?seed=<%= encodeURIComponent(user.email) %>"
    alt="avatar"
    style="height:2rem; width:2rem; border-radius:50%; vertical-align:middle; margin-right:0.5em;"
  />
        <class="welcome-msg"> Hi, <%= user.email %>!
         </span>
        <a href="/auth/sign-out">Sign Out</a>
      <% } %>
    </div>
  </nav>
  <main>
